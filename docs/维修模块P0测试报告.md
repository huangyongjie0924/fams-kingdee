# 维修流程模块 P0 独立测试报告

| 项 | 内容 |
|---|---|
| 项目 | FAMS · 固定资产台账管理系统（生产在跑） |
| 被测提交 | `f4403f9` feat(repair): 实施维修流程模块 P0 最小闭环（T01~T05） |
| 测试人 | 严过关（QA 工程师）· 独立验证（全新上下文，未采信工程师自报结论） |
| 测试日期 | 2026-09-20 |
| 测试方式 | **实测**：真库集成测试（Go + httptest 打真实 HTTP 路由）+ 源码静态核对 + 构建/回归 |
| 数据库 | MySQL 内网生产实例 / `asset` 库（**生产库**，所有探针数据自清理） |
| 测试轮次 | 第 1 轮（本轮全绿，无需第 2 轮） |

---

## 0. 结论摘要

**结论：P0 交付通过独立验证。** 8 项必验清单全部通过，4 项用户决策全部落地。**未发现 P0/P1/P2 缺陷。**

| 指标 | 数值 |
|---|---|
| 验证项 | 8 项必验 + 4 项决策 = 12 项，**通过 12 / 失败 0** |
| 新增测试用例 | 10 个（store 4 + api 6），**全部 PASS** |
| 现有测试套件 | `go test ./...` **全绿** |
| P0 阻塞缺陷 | **0** |
| P1 严重缺陷 | **0** |
| P2 一般缺陷 | **0** |
| 非阻塞观察 | 2 条（见 §5，均为既有设计/既有行为，非本次引入） |
| **智能路由判定** | **NoOne（全部通过，报告成功）** |

---

## 1. 验证环境与方法

- 构建顺序按项目约定：先 `cd web && npm run build`，再 `go build`（`go:embed all:web/dist`）。
- 跑 Go 命令前：`set -a && . ./.env && set +a`（`config/config.go` 只认 `os.Getenv`）。
- 关键方法：**不只看代码**——新增两套连真库的集成测试，直接打真实 HTTP 路由与真实同步写路径。

新增测试文件（均自带清理，可保留复用）：

| 文件 | 覆盖 |
|---|---|
| `store/repair_qa_store_test.go` | 同步隔离（R1）、display_status 四处一致、store 层非法跃迁、数据卫生 |
| `api/repair_qa_api_test.go` | HTTP 全闭环、权限矩阵、可见范围、IDOR 上传、同步权限漂移、display_status API 契约 |

清理纪律：所有探针数据用 `QA-REPAIR-` / `ZZ-REPAIR-` 前缀 + `qa` 账号，测试内 `t.Cleanup` 按 id 删除；**全程无 DROP/TRUNCATE/无 WHERE 的 DELETE**。

---

## 2. 八项必验清单逐项结果

### ① 状态机全路径 —— ✅ 通过

**合法流转放行**（实测闭环，API 层）：

```
$ go test ./api/... -run TestQARepairAPIEndToEnd -v
    viewer 提交成功：code=WX202609200006 status=pending
    闭环推进 → accepted
    闭环推进 → dispatched
    闭环推进 → repairing
    闭环推进 → confirming
    闭环推进 → done
--- PASS: TestQARepairAPIEndToEnd
```

**非法跃迁必须被拒**（实测，store 层直击 `Transition` 单一入口）：

```
$ go test ./store/... -run TestQAStoreLevelIllegalTransitions -v
    pending + take      → 正确拒绝: 当前状态「待受理」不允许「接单」操作
    pending + finish    → 正确拒绝: 当前状态「待受理」不允许「报完工」操作
    pending + dispatch  → 正确拒绝: 当前状态「待受理」不允许「派工」操作
    pending + confirm   → 正确拒绝: 当前状态「待受理」不允许「确认完工」操作
    pending + return    → 正确拒绝: 当前状态「待受理」不允许「退回」操作
--- PASS: TestQAStoreLevelIllegalTransitions
```

- 另测：`已完工` 后再 `take` / `accept` 均被拒（终态不可流转）——**通过**。
- 状态机真相在 `store/repair.go:137-157 repairTransitions`，`Transition()`（L165-237）为唯一入口，非法跃迁走 `to, ok := repairTransitions[action][cur.Status]` 判定（L181-185）。**store 层确有跃迁校验**，工程师没有漏。

**分支**：

| 分支 | 实测结果 |
|---|---|
| 驳回必填原因 | 空原因 → `400 驳回必须填写原因`；带原因 → `rejected` ✅ |
| 员工撤单（受理前） | `pending` + 报修人撤单 → `cancelled` ✅ |
| 员工撤单（受理后） | `accepted` 后报修人撤单 → **403**（受理后只能管理员取消）✅；管理员取消 → `cancelled` ✅ |
| 员工退回 | 空原因 → `400`；带原因 → 回到 `repairing`，且卡 `biz_status` 再次为「维修中」✅ |

> 说明：API 层「待受理 + take」被拒返回的是 **403**（先撞上 `handleTakeRepair` 的「只有被指派的维修工可以接单」对象级校验，此时无 assignee），非 400。**拒绝结果正确**；状态机层面的拒绝已由 store 层用例单独证成。

### ② `biz_status` 与星瀚同步的隔离（本次最高风险）—— ✅ 通过

**实测方式**：直接调用**真实同步写路径** `store.ApplyOwnedCardTx(tx, src, op, dryRun=false, nil)`，对一张 `biz_status='维修中'` 的探针卡执行一次「星瀚卡落库」。

```
$ go test ./store/... -run TestQASyncIsolationOfBizStatus -v
    同步 action=updated；同步后 status="闲置" biz_status="维修中" display_status="维修中" name="星瀚同步改名"
--- PASS: TestQASyncIsolationOfBizStatus
```

- ✅ **`biz_status` 未被改动、未被清空**（仍为「维修中」）。
- ✅ **`status`（星瀚托管列）被同步覆盖**（在用 → 闲置），符合设计。
- ✅ 托管字段 `name` 被覆盖 → 证明同步写路径确实执行了。

**结构性保证**（静态核对）：`store/sync.go` 的 `kingdeeOwnedColumns`（L216-224）**不含** `biz_status`；`ApplyOwnedCardTx` 的 UPDATE 只拼 `kingdeeOwnedColumns` + `ext_json`（L466-473）；`mergeOwnedFields`（L295-371）逐字段赋值、无 `biz_status`。因此同步**结构上无法**清空 `biz_status`。

**守卫测试有效性验证**（按任务要求故意注入）：

```
# 故意把 "biz_status" 加进 kingdeeOwnedColumns
$ go test ./store/... -run TestBizStatusNotInOwnedColumnLists -v
    repair_guard_test.go:28: kingdeeOwnedColumns 不能包含 biz_status
--- FAIL: TestBizStatusNotInOwnedColumnLists     ← 守卫有效，能挡住
# 还原后
$ go test ./store/... -run TestBizStatusNotInOwnedColumnLists -v
--- PASS: TestBizStatusNotInOwnedColumnLists
$ git diff --stat store/sync.go     ← 空，已干净还原
```

> **关于「实际跑一次同步」的说明（重要）**：未执行 `cmd/dryrun`/`cmd/orgsync` 全量跑批。原因是即使 dry-run，`syncer.Run` 仍会在 L104 `CreateSyncRun` 并在 L210 `tx.Commit()` 提交 **sync_run 审计行**、可能推进 **sync_state 游标**——这是**生产库写入，无法干净回滚**。改用**等价的、可回滚的**方式：直接对探针卡调用真实写路径函数（即全量同步对每张卡所做的事），既拿到实测证据又不污染生产。此替代方案已覆盖 R1 的全部验收点。

### ③ `display_status` 一致性（架构师最担心的漏改点）—— ✅ 通过

造一张 `biz_status='维修中'` 的卡，四处口径实测一致：

```
$ go test ./store/... -run TestQADisplayStatusConsistency -v
--- PASS: TestQADisplayStatusConsistency
```

| # | 显示位置 | 实测结果 |
|---|---|---|
| 1 | 资产列表 `List.vue`（桌面 + 移动**两处**） | `row.status` → `row.display_status`（diff 已核对两处）✅ |
| 2 | 资产详情 `CardDetail.vue` | `card.display_status` ✅ |
| 3 | 按状态筛选 `store/card.go buildWhere` | 「筛维修中」能筛到该卡，且筛出行的 `display_status`=「维修中」；反向「筛在用」不含该卡 ✅ |
| 4 | 导出 Excel `api/importexport.go handleExport` | 导出「状态」列写 `c.DisplayStatus`（L98），实测导出接口 200、字节数 32705 ✅ |

API 层契约亦实测（`TestQADisplayStatusViaAPI`）：`GET /api/assets/{id}` 与 `GET /api/assets?keyword=` 返回的 `display_status` 均为「维修中」。

架构文档 §1.5 列的 9 个改动点逐点核对，**全部落地**（mysql 迁移列 / model 字段 / cardSelect / scanCard / buildWhere / sortColumns / export / List.vue×2 / CardDetail.vue）。单点收敛实现：`store/card.go effectiveStatus()`，与 SQL 里的 `COALESCE(NULLIF(c.biz_status,''), c.status)` 逐字对应。

### ④ 权限矩阵（5 角色实测）—— ✅ 通过

```
$ go test ./api/... -run TestQARepairPermissionMatrix -v
    ✓ viewer 受理 → 403      ✓ viewer 派工 → 403
    ✓ viewer 接单 → 403      ✓ viewer 报完工 → 403
    ✓ repair_tech 受理 → 403 ✓ repair_tech 派工 → 403
    ✓ dept_head 受理 → 403   ✓ dept_head 派工 → 403
    # 正向：asset_manager 受理 → 200；admin 受理 → 200
--- PASS: TestQARepairPermissionMatrix
```

**可见范围**（`TestQARepairVisibilityScope`，实测）：

| 角色 | 期望 | 实测 |
|---|---|---|
| viewer A | 只见自己提交的单 | ✅ 只见 A，不见 B |
| viewer B | 只见自己提交的单 | ✅ |
| repair_tech | 只见派给自己的单 | ✅ 只见派给自己的 B |
| dept_head | 本部门子树 | ✅ 只见本部门内的 A（B 无部门，不可见） |
| asset_manager / admin | 全部 | ✅ 见 A+B |

越权点查：viewer A 取 B 单详情 → **403** ✅。`mine=1` 只返回本人 ✅。
前端按钮不显示：`Detail.vue` 各按钮可见性由 `auth.can()` + 状态 + 是否当事人共同决定（L200-219），与后端对象级鉴权口径一致。

### ⑤ 权限映射两份一致 —— ✅ 通过

用脚本逐项比对 `model/model.go rolePermissions` 与 `web/src/stores/auth.ts rolePermissions`：

```
角色             后端  前端    一致性
asset_manager  8   8     ✅ 一致
counter        2   2     ✅ 一致
dept_head      2   2     ✅ 一致
repair_tech    1   1     ✅ 一致
viewer         1   1     ✅ 一致
全部一致： True
asset_manager 前后端均无 sync.manage： True
```

- 覆盖新角色 `repair_tech`：后端 `rolePermissions` 有、前端有、`api/user.go roleBindingErr` 有（必须绑员工）、`api/scope.go repairScope` 有（只看派给自己的单）、`web/src/views/users/UserList.vue` 下拉有。**四处齐全**。

### ⑥ 上传的对象级鉴权（IDOR 防护）—— ✅ 通过

```
$ go test ./api/... -run TestQARepairUploadIDOR -v
    ✓ 自己的单上传成功 id=3
    ✓ 他人单据上传被拒 403
    ✓ 未登录上传被拒 401
    ✓ 旧 /api/upload 对 viewer 仍 403（未放宽）
--- PASS: TestQARepairUploadIDOR
```

- 员工 A 给自己的单上传 → 200 ✅
- 员工 A 给员工 B 的单上传 → **403** ✅
- 未登录 → **401** ✅
- 旧 `/api/upload` **未放宽**：viewer 仍 403（需 `asset.manage`），asset_manager 仍 200 ✅
- 独立路由 `/api/repair-upload` 三层防护（认证 / `requireAnyPerm(repair.report, repair.handle)` / 对象级）见 `api/repair_upload.go`，且归属以单据为准、忽略表单篡改的 `card_id`。

### ⑦ 数据卫生（生产库）—— ✅ 通过

```
$ go test ./store/... -run TestQADataHygiene -v
    repair_order: 总 0，疑似测试残留 0
    doc_status_log(repair): 总 0，疑似测试残留 0
    repair_attachment: 总 0，疑似测试残留 0
    探针卡残留（asset_card 中 ZZ-REPAIR/QA-REPAIR）: 0
--- PASS: TestQADataHygiene
```

- 工程师的真库 E2E 测试（`store/repair_e2e_test.go`）**清理干净**：三张表均无 `created_by='e2e'` / `ZZ-REPAIR` 残留。
- 我自己的测试数据同样清理干净（三表 0 残留、探针卡 0 残留、`uploads/` 目录无孤儿文件）。
- 全程未碰真实业务数据。

### ⑧ 回归 —— ✅ 通过

```
$ go test ./...
ok  	asset-mgr/api	5.054s
ok  	asset-mgr/cmd/kdscan / kdverify / orgreset
ok  	asset-mgr/config
ok  	asset-mgr/store
ok  	asset-mgr/syncer
```

- **`cardSelect`/`scanCard` 增列未破坏查询**：实测 `GET /api/assets`（列表）、`GET /api/assets/{id}`（详情）、`GET /api/assets/export`（导出）、按状态筛选均 200 且数据正确（列数匹配）。
- 资产卡列表/详情/编辑/导出/标签、盘点、主数据、用户管理相关测试全绿。
- **构建**：`go build ./...` 与主程序 `go build .`（含 `go:embed all:web/dist`）均成功，产出 18MB 二进制。
- 前端 `npm run build` 成功（1761 modules，8.0s）。

---

## 3. 四项用户决策验证

| # | 决策 | 结果 | 证据 |
|---|---|---|---|
| 1 | `viewer` 授予 `repair.report` | ✅ 落地 | viewer 实测提交报修 200；同时受理/派工实测 403。后端 `model.go` 与前端 `auth.ts` 均给 viewer 授 `repair.report` |
| 2 | P0 包含「确认完工」 | ✅ 落地 | 实测完整闭环到 `done`；`handleConfirmRepair` + 路由 `POST /api/repairs/{id}/confirm`（需 `repair.report`）均在；前端 `Detail.vue` 有「确认完工」按钮 |
| 3 | 状态下拉移除「维修中/调拨中」 | ✅ 落地（区分合理） | 批量调整 `List.vue` 用 `selectableStatuses()`、单卡编辑 `CardForm.vue` 用 `statusOptions`（均排除两值）；`SearchDrawer.vue` **保留**全量枚举。**该区分合理**：筛选「维修中」是有效查询（biz_status 可为维修中），若也从筛选移除，维修中的卡将无法被筛出——保留是**必要**而非遗漏。用户拍板原意（不让用户手填这两个值）被满足 |
| 4 | 权限映射漂移修复 | ✅ 落地 | 前后端均无 `sync.manage`（脚本证成）；`SideMenu.vue` 同步菜单 `v-if="auth.can('sync.manage')"` → asset_manager 不显示；后端同步三接口实测对 asset_manager 仍 403（见下） |

决策 4 补充实测：

```
$ go test ./api/... -run TestQASyncPermissionNoDrift -v
    ✓ asset_manager /api/sync/run → 403
    ✓ asset_manager /api/sync/test-connect → 403
    ✓ asset_manager /api/sync/org → 403
--- PASS: TestQASyncPermissionNoDrift
```

> 「不再 403」的达成路径 = **前端按钮不再显示**（漂移修复）→ 用户点不到；后端仍 403 属纵深防御，正确保留。

---

## 4. 缺陷清单

**无。** 未发现任何 P0 阻塞 / P1 严重 / P2 一般缺陷。

---

## 5. 非阻塞观察（非本次引入，供参考，不影响判定）

1. **路由无权限守卫**：`web/src/router/index.ts` 的 `beforeEach` 只校验登录态，不校验角色权限。理论上受限账号可**直接改 URL** 进入 `/sync`、`/repairs` 等页面（菜单已隐藏），页面加载后调写接口会得到 403。此为**既有设计**（菜单隐藏 + 后端 403），非本次提交引入，本次未扩大该面。
2. **`CardForm` 编辑 `status` 对「维修中」卡的表现**：卡若 `biz_status='维修中'`，其 `form.status` 仍是「在用」；管理员在卡片编辑里改 `status` 不会改变列表/详情显示的「维修中」（biz_status 优先，且 biz_status 只由单据写）。符合「状态真相由单据驱动」的设计约定，但可能让管理员困惑，建议后续在 UI 上加一句说明。

---

## 6. 智能路由判定

### 判定：`NoOne`

**理由**：8 项必验清单 + 4 项用户决策全部通过；10 个新增用例全绿；现有测试套件全绿；构建（前端 + Go embed）通过；无 P0/P1/P2 缺陷。

- 未发现源码 Bug（无需路由到 **Engineer**）。
- 未发现测试代码自身的 Bug（无需路由到 **QA**）。
- 未发现工程师报告与实际不符（自报 `IS_PASS: YES`、硬约束均**属实**；其真库 E2E 测试清理干净、守卫测试确实能挡住注入）。

---

## 7. 附录：本次新增测试与清理说明

| 文件 | 用例 | 说明 |
|---|---|---|
| `store/repair_qa_store_test.go` | `TestQASyncIsolationOfBizStatus`、`TestQADisplayStatusConsistency`、`TestQAStoreLevelIllegalTransitions`、`TestQADataHygiene` | 连真库；探针前缀 `QA-REPAIR-`；`t.Cleanup` 自清理 |
| `api/repair_qa_api_test.go` | `TestQARepairAPIEndToEnd`、`TestQARepairPermissionMatrix`、`TestQARepairVisibilityScope`、`TestQARepairUploadIDOR`、`TestQASyncPermissionNoDrift`、`TestQADisplayStatusViaAPI` | httptest 打真实路由；探针卡/员工/账号自清理；上传文件自删除 |

**遗留物核查**：`git status` 仅新增上述两个测试文件（未改动任何业务源码）；`store/sync.go` 注入实验已 `git diff` 确认为空；`uploads/` 无孤儿文件；维修三表 0 残留。

---

## 8. 复验（需求 A「可维修分类」落地后，2026-09-20）

**触发**：增量需求 A（commit `ac784d9` + `2707516`）引入「仅设备类（可维修分类）资产可提交维修单」三层拦截，并**改动了我的 `api/repair_qa_api_test.go` 探针 fixture**。

### 8.1 工程师对我测试文件的改动 —— 已核，判定「合理且未削弱断言」

- 改动**仅**限 `newCard`：新增 `repairableCategoryID()`（查首个 `repairable=1` 分类），探针卡 INSERT 增挂 `category_id`。
- **未改动任何断言**；状态机/权限/可见范围/IDOR 用例语义不变。判定：需求 A 的分类门控与这些用例正交，只修 fixture 是正确做法（否则被门控误伤成假红）。

### 8.2 复跑结果 —— 全绿

```
$ go test ./...            # 连续两次，稳定
ok asset-mgr/api / cmd/kdscan / cmd/kdverify / cmd/orgreset / config / store / syncer   （7 包全 ok，无 FAIL）
$ cd web && npm run build  # ✓ built in 23.86s
$ go build ./... && go build .   # 成功（含 go:embed all:web/dist）
```

- 我的 11 个 QA 用例全绿（原 10 个 + 新增分类门控用例）。
- 数据卫生：维修三表 0 残留、无孤儿文件。

### 8.3 需求 A 后端门控独立验证（我自写用例 `TestQARepairCategoryGate`）

```
$ go test ./api/... -run TestQARepairCategoryGate -v
    ✓ 可维修分类 → 200（code=WX202609200119）
    ✓ 不可维修分类 → 400：「房屋及建筑物」类资产不支持报修，如需维修请联系资产管理员
    ✓ category_id=0 → 400：「」类资产不支持报修，如需维修请联系资产管理员
--- PASS: TestQARepairCategoryGate
```

- 正向放行、反向拦截（后端是底线、前端可绕过）、`category_id=0` 哨兵值走 `COALESCE` 兜底 **不 500** —— 三条均符合设计。
- 真库分类状态实测：`0203 机器设备 / 0204 运输工具 / 0205 电子设备 / 0206 办公设备` = `repairable=1`，其余（0201/0202/0299/0101/0102/0302）= 0。**分类 id 与 code 错位**（如 0205=id3、0299=id7），证明迁移按 `code` 回填是**必要且正确**的。
- 守卫测试有效：`store/category_guard_test.go` 扫同步链路源码禁 `UPDATE asset_category`；`TestRepairableNotInCardColumnLists` 禁 `repairable` 混入卡片列清单；`store/card_repairable_test.go` 连真库钉死派生字段链路。

### 8.4 复验缺陷

- **P0/P1/P2：0**。
- 非阻塞观察（新增 1 条）：`category_id=0` 哨兵值被拦时，文案里的分类名是空串（「」类资产不支持报修）。属**极端边界**（正常业务卡都挂分类），仅影响提示可读性，建议后续对空分类名做兜底文案（如「未设置分类」）。**非本次需求范围，不阻塞。**

### 8.5 数据卫生：QA 自查发现并修复一处自身泄漏（已闭环）

复验时用「孤儿行」不变量（`doc_status_log` / `repair_attachment` 指向不存在的 `repair_order`）复查生产库，**发现并修复了 QA 自己的一处测试残留**：

- **根因**：我为需求 A 分类门控新写的辅助 `newCardWithCategory` 的 `t.Cleanup` 漏删了 `doc_status_log`（只删了单/卡/履历），导致多次跑测累计留下 **5 条孤儿流转记录**（`operator='QA报修人'`、`doc_id∈{119,131,143,159,173}`）。
- **修复**：① 补全 `newCardWithCategory` 清理（对齐原 `newCard`，同时删 `doc_status_log` + `repair_attachment`）；② 加固 `TestQADataHygiene`，新增**孤儿行不变量**断言（不依赖命名前缀，任何指向不存在维修单的从属行都会被抓）；③ 定向清理已产生的 5 条孤儿记录。
- **验证**：修复后连跑全量套件，孤儿行 = 0，且不再新增。此前的残留计数口径（只按 `created_by/operator IN (e2e,qa)`）漏掉了 `'QA报修人'`，是本次自查暴露的测试自身缺陷，已一并加固。

### 8.6 生产库发现他人手工冒烟残留（非 QA 数据，待处置）

排查时发现 **1 条非 QA/非 E2E 的遗留维修单**：

```
ORDER id=65 code=WX202609200065 card_id=199 asset_code=12010205000007
      reporter=127/黄永洁 status=dispatched created_by=黄永洁 at=2026-09-20 14:06:29
      （含 submit/accept/dispatch 3 条流转记录）
```

- 判断：**人工冒烟测试**遗留（14:06–14:08 由开发者账号连续 submit→accept→dispatch），落在**真实资产卡 199** 上，未清理。
- 影响：该真实资产名下多出一条「已派工」维修单（当前 `status=dispatched`，故 `biz_status` 未被置「维修中」，未污染卡状态；但单据本身是脏数据）。
- 处置：**非 QA 数据，未擅自删除**，已在回传中明确点名，请单据创建者/团队确认后清理（如需我可代删：`DELETE FROM doc_status_log WHERE doc_type='repair' AND doc_id=65; DELETE FROM repair_order WHERE id=65;`）。
- **后续（终态）**：QA 再次独立复扫时，`repair_order` 总数已为 **0**，该单（id=65）**已不复存在**——**非 QA 删除**（QA 全程未对其执行任何 DELETE；QA 的孤儿行清理只删「无对应单据」的日志，该单当时有单据故不受影响）。删除方未知，属其他成员/人工操作。至 T05 前，生产库维修相关表**全部为 0、无残留**（见 §8.8 终态快照）。若该单确为业务/验收留痕，请团队向创建者核实是否需恢复。

### 8.7 环境说明

- 复跑期间 `go test ./...` 曾出现一次瞬时 `[build failed]`（全部包），系与后台 `npm run build` 争用 `web/dist`（`go:embed`）所致；**随后连续两次干净复跑均全绿**，非代码缺陷。
- 复验未改动任何业务源码；`api/repair_qa_api_test.go` 新增 1 个用例（`TestQARepairCategoryGate` + `newCardWithCategory` 辅助），`store/repair_qa_store_test.go` 保持不变。

### 8.8 终态数据卫生快照（QA 独立复扫，只读）

```
repair_order 总数=0
doc_status_log(repair)=0 孤儿=0
repair_attachment=0 孤儿=0
探针账号(zz-/qa-)残留=0
探针卡(ZZ-/QA-)残留=0
探针员工残留=0
```

结论：截至复扫时点，生产库维修相关表**全部为 0、无任何测试残留**。engineer-3 亦自查清理了其 T03 harness 遗留的 1 个 `zz-*` 探针账号（根因：`defer st.Close()` 早于 `t.Cleanup` 执行，清理 DELETE 打在已关闭连接池上静默失败）。**QA 的测试夹具未受该根因影响**——`api` harness 的 `st.Close()` 与各夹具清理均经 `t.Cleanup` 注册（LIFO，清理先于 Close）；`store` 测试统一用 `defer`（同为 LIFO，清理先于 Close），顺序均正确。

### 8.9 路由判定（复验）

**NoOne** —— 需求 A 复验全绿，无 P0/P1/P2 缺陷；生产库无残留。
